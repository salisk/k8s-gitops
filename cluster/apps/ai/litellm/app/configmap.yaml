---
apiVersion: v1
kind: ConfigMap
metadata:
  name: litellm-configmap
  namespace: ai
data:
  config.yaml: |
    model_list:
      - model_name: "kimchi/glm-5.2-fp8"
        litellm_params:
          model: "openai/glm-5.2-fp8"
          api_base: os.environ/KIMCHI_URL
          api_key: os.environ/KIMCHI_API_KEY
      - model_name: "kimchi/kimi-k2.7"
        litellm_params:
          model: "openai/kimi-k2.7"
          api_base: os.environ/KIMCHI_URL
          api_key: os.environ/KIMCHI_API_KEY
      - model_name: "kimchi/minimax-m3"
        litellm_params:
          model: "openai/minimax-m3"
          api_base: os.environ/KIMCHI_URL
          api_key: os.environ/KIMCHI_API_KEY

    general_settings:
      health_check_endpoint: /v1/health
      database_connection_pool_limit: 10
      disable_error_logs: True
      allow_requests_on_db_unavailable: True
      use_redis_transaction_buffer: true

    router_settings:
      routing_strategy: usage-based-routing-v2
      redis_host: os.environ/REDIS_HOST
      redis_port: os.environ/REDIS_PORT

    litellm_settings:
      success_callback: ["prometheus"]
      failure_callback: ["prometheus"]
      prometheus_adapter: true
      cache: True
      cache_params:
        type: redis
        host: os.environ/REDIS_HOST
        port: os.environ/REDIS_PORT
        namespace: litellm
      drop_params: true
      set_verbose: false
      json_logs: true
      request_timeout: 600
      num_retries: 2
      retry_after: 5

    environment:
      - REDIS_HOST
      - REDIS_PORT
