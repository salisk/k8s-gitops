---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/v1/secret.json
apiVersion: v1
kind: Secret
metadata:
  name: hermes-secret
  namespace: ai
type: Opaque
stringData:
  # LiteLLM API Key (same master key used by Open WebUI)
  LITELLM_API_KEY: "${LITELLM_MASTER_KEY}"
