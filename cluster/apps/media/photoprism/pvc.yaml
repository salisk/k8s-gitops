apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: photoprism-config-v1
spec:
  storageClassName: longhorn
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 10Gi
---
