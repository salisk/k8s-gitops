---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: soulsync
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      soulsync:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              # renovate: datasource=docker depName=boulderbadgedad/soulsync
              repository: boulderbadgedad/soulsync
              tag: 2.1@sha256:70ff3a770ec5fe717c7e4e9c6703465ea4696de7804098bba0e569ffd8f4ad30
            env:
              TZ: ${TIMEZONE}
              PUID: 1000
              PGID: 1000
              UMASK: "022"
              FLASK_ENV: production
              PYTHONPATH: /app
              SOULSYNC_CONFIG_PATH: /app/config/config.json
            probes:
              liveness: &probes
                enabled: true
                spec:
                  httpGet:
                    path: /
                    port: 8888
                  initialDelaySeconds: 30
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 500m
                memory: 512Mi
              limits:
                memory: 2Gi
    service:
      app:
        controller: soulsync
        ports:
          http:
            port: &port 8008
          spotify:
            port: 8888
          tidal:
            port: 8889
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      config:
        enabled: true
        existingClaim: soulsync-config
        globalMounts:
          - path: /app/config
            subPath: config
          - path: /app/logs
            subPath: logs
          - path: /app/data
            subPath: data
      media:
        enabled: true
        existingClaim: nfs-media-pvc-v2
        globalMounts:
          - path: /app/downloads
            subPath: downloads/soulseek/complete
          - path: /app/Transfer
            subPath: downloads/soulseek/shared
          - path: /app/Staging
            subPath: downloads/soulseek/staging
      tmpfs:
        type: emptyDir
        advancedMounts:
          soulsync:
            app:
              - path: /tmp
