---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: soulsync
spec:
  interval: 30m
  chartRef:
    kind: OCIRepository
    name: app-template
    namespace: flux-system
  maxHistory: 3
  install:
    createNamespace: true
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    controllers:
      soulsync:
        annotations:
          reloader.stakater.com/auto: "true"
        containers:
          app:
            image:
              repository: boulderbadgedad/soulsync
              tag: latest@sha256:c85beb8db2ee40440fff89d7f7800a65417d42abf9e78e124db064412a9c34bd
            env:
              TZ: ${TIMEZONE}
              PUID: 1000
              PGID: 1000
              UMASK: "022"
              FLASK_ENV: production
              PYTHONPATH: /app
              SOULSYNC_CONFIG_PATH: /config/config.json
            probes:
              liveness: &probes
                enabled: true
                spec:
                  httpGet:
                    path: /
                    port: 8888
                  initialDelaySeconds: 60
                  periodSeconds: 30
                  timeoutSeconds: 10
                  failureThreshold: 3
              readiness: *probes
            resources:
              requests:
                cpu: 500m
                memory: 512Mi
              limits:
                memory: 2Gi
    service:
      app:
        controller: soulsync
        ports:
          http:
            port: &port 8008
          spotify:
            port: 8888
          tidal:
            port: 8889
    route:
      app:
        hostnames:
          - "{{ .Release.Name }}.${SECRET_DOMAIN}"
        parentRefs:
          - name: internal
            namespace: kube-system
            sectionName: https
        rules:
          - backendRefs:
              - identifier: app
                port: *port
    persistence:
      config:
        enabled: true
        existingClaim: soulsync-config
        globalMounts:
          - path: /config/config.json
            subPath: config/config.json
          - path: /app/database/music_library.db
            subPath: database/music_library.db
      music-library:
        enabled: true
        existingClaim: nfs-media-pvc-v2
        globalMounts:
          - path: /app/downloads
            subPath: downloads/soulseek
          - path: /app/Transfers
            subPath: downloads/soulseek-transfers
      tmpfs:
        type: emptyDir
        advancedMounts:
          soulsync:
            app:
              - path: /config/log
                subPath: log
              - path: /tmp
                subPath: tmp
