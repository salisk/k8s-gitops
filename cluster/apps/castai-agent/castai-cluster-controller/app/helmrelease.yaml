---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: cluster-controller
spec:
  interval: 1h
  chart:
    spec:
      chart: castai-cluster-controller
      version: 0.91.0
      sourceRef:
        kind: HelmRepository
        name: castai-charts
        namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    resetValues: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    castai:
      apiKey: ${CASTAI_API_KEY}
      apiURL: ${CASTAI_API_URL}
      clusterID: ${CASTAI_CLUSTER_ID}
    enableTopologySpreadConstraints: true
