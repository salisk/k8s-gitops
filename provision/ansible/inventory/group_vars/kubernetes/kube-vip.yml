---
# (string) The interface on the host kube-vip should attach to
kubevip_interface: "eno1"
# (string) The ARP address kube-vip broadcasts
# kubevip_address: "192.168.0.251"
kubevip_address: "192.168.30.251"
